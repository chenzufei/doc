
1、安装openssh
apt install openssh-server

2、修改配置文件/etc/ssh/sshd_config
将
#PermitRootLogin prohibit-password          //表示运行root登陆，但不支持以密码方式登陆，只能以公钥方式登陆。
修改为
PermitRootLogin yes

3、重启服务
sudo systemctl restart ssh